# Asset Catalog data history

`catalog.json` is the human-readable catalog history deployed to GitHub Pages.
`manifest.json` is the human-readable Pages manifest history.
`crypto/catalog.json` is the human-readable cryptocurrency catalog history.
`crypto/manifest.json` is the human-readable cryptocurrency manifest history.
All files are generated by GitHub Actions; review changes with GitHub or `git diff`.
